# Docker 배포 가이드

## 📋 목차
1. [Docker 개념 이해](#1-docker-개념-이해)
2. [프로젝트 Docker 파일 설명](#2-프로젝트-docker-파일-설명)
3. [로컬 Docker 배포 순서](#3-로컬-docker-배포-순서)
4. [실제 서비스 배포 옵션](#4-실제-서비스-배포-옵션)
5. [트러블슈팅](#5-트러블슈팅)

---

## 1. Docker 개념 이해

### Docker 이미지 (Image)
- **정의**: 애플리케이션 + 실행환경(OS, 런타임, 라이브러리)을 **패키징한 템플릿**
- **비유**: 붕어빵 **틀** - 같은 틀로 동일한 붕어빵을 여러 개 찍어낼 수 있음
- **특징**: 읽기 전용, 변경 불가, 레이어 구조

```
이미지 = 코드 + JDK 17 + Alpine Linux + 설정파일들
```

### Docker 컨테이너 (Container)
- **정의**: 이미지를 **실행한 인스턴스** (실제 동작하는 격리된 프로세스)
- **비유**: 틀에서 찍어낸 **붕어빵** - 실제로 먹을 수 있음
- **특징**: 독립적인 파일시스템, 네트워크, 프로세스 공간

```
컨테이너 = 이미지 + 실행 상태 (메모리, CPU, 네트워크)
```

### 로컬 vs 클라우드 배포 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                        개발 PC (로컬)                            │
├─────────────────────────────────────────────────────────────────┤
│  1. Dockerfile + 소스코드                                        │
│       ↓                                                          │
│  2. docker-compose build  →  Docker 이미지 생성                  │
│       ↓                                                          │
│  3. docker-compose up -d  →  컨테이너 실행 (localhost:8080)      │
└─────────────────────────────────────────────────────────────────┘

                            ↓ docker push (이미지 업로드)

┌─────────────────────────────────────────────────────────────────┐
│                     Docker Hub (이미지 저장소)                    │
│   - hub.docker.com                                               │
│   - 내 이미지 주소: docker.io/내아이디/doll_gacha:latest          │
└─────────────────────────────────────────────────────────────────┘

                            ↓ docker pull (이미지 다운로드)

┌─────────────────────────────────────────────────────────────────┐
│               클라우드 서버 (AWS, Railway, Render 등)            │
│   - 이미지를 다운받아 컨테이너로 실행                             │
│   - 공인 IP/도메인으로 외부 접근 가능                             │
└─────────────────────────────────────────────────────────────────┘
```

### Railway/Render의 자동화 방식
```
GitHub 저장소 연결 → 자동으로 Dockerfile 감지 → 이미지 빌드 → 컨테이너 실행
(Docker Hub 없이 바로 배포 가능!)
```

---

## 2. 프로젝트 Docker 파일 설명

### 📄 Dockerfile
> 이미지를 만드는 **레시피(설계도)**

```dockerfile
# ===== Stage 1: 빌드 스테이지 =====
# Gradle + JDK 17 환경에서 소스코드를 JAR로 빌드
FROM gradle:8-jdk17 AS builder

WORKDIR /build

# Gradle 파일 먼저 복사 (캐싱 활용 - 의존성 변경 없으면 재사용)
COPY build.gradle settings.gradle ./
COPY gradle ./gradle

# 의존성 다운로드 (캐싱)
RUN gradle dependencies --no-daemon || true

# 소스코드 복사 및 빌드 (테스트 제외)
COPY src ./src
RUN gradle bootJar --no-daemon -x test

# ===== Stage 2: 실행 스테이지 =====
# 가벼운 Alpine Linux + JRE만으로 실행
FROM eclipse-temurin:17-jre-alpine

WORKDIR /app

# 시간대 설정 (한국)
ENV TZ=Asia/Seoul
RUN apk add --no-cache tzdata curl && \
    cp /usr/share/zoneinfo/$TZ /etc/localtime

# 업로드 디렉토리 생성
RUN mkdir -p /app/uploads

# 빌드된 JAR 파일 복사
COPY --from=builder /build/build/libs/*.jar app.jar

EXPOSE 8080

# 프로덕션 프로파일로 실행
ENTRYPOINT ["java", "-jar", "-Dspring.profiles.active=prod", "app.jar"]
```

**Multi-stage 빌드의 장점:**
- Stage 1에서 빌드 도구(Gradle) 사용 후 버림
- Stage 2에서는 JRE만 포함 → 이미지 크기 대폭 감소 (1GB → ~200MB)

---

### 📄 docker-compose.yml
> 여러 컨테이너를 **함께 관리하는 오케스트레이션 파일**

```yaml
services:
  # 1. MariaDB 데이터베이스 컨테이너
  db:
    image: mariadb:10.11          # Docker Hub에서 공식 이미지 사용
    container_name: doll_gacha_db
    restart: unless-stopped       # 서버 재시작 시 자동 실행
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: doll_gacha
      MYSQL_USER: doll_gacha
      MYSQL_PASSWORD: ${DB_PASSWORD:-ckdlsk13!}
    ports:
      - "3407:3306"               # 호스트 3407 → 컨테이너 3306
    volumes:
      - db_data:/var/lib/mysql    # 데이터 영구 저장
    healthcheck:                  # DB 준비 완료 체크
      test: ["CMD", "healthcheck.sh", "--connect"]
      interval: 10s

  # 2. Spring Boot 애플리케이션 컨테이너
  app:
    build:
      context: .
      dockerfile: Dockerfile      # 위의 Dockerfile 사용
    container_name: doll_gacha_app
    ports:
      - "8080:8080"               # 호스트 8080 → 컨테이너 8080
    environment:
      # DB 접속 정보 (db는 docker 내부 DNS 이름)
      SPRING_DATASOURCE_URL: jdbc:mariadb://db:3306/doll_gacha
      # OAuth2 설정 (.env 파일에서 읽음)
      KAKAO_CLIENT_ID: ${KAKAO_CLIENT_ID}
      KAKAO_CLIENT_SECRET: ${KAKAO_CLIENT_SECRET}
      # JWT 설정
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    volumes:
      - ./uploads:/app/uploads    # 업로드 파일 영구 저장
    depends_on:
      db:
        condition: service_healthy  # DB 준비 완료 후 시작

volumes:
  db_data:                        # 명명된 볼륨 (Docker가 관리)
```

**핵심 개념:**
- `services`: 각 컨테이너 정의
- `volumes`: 데이터 영구 저장 (컨테이너 삭제해도 데이터 유지)
- `depends_on`: 컨테이너 시작 순서 제어
- `${변수}`: `.env` 파일에서 값 읽기

---

### 📄 .env
> 환경변수 파일 (민감한 정보 저장, **Git에 올리면 안 됨!**)

```env
JWT_SECRET_KEY=36bbeabc13b21db1aa0df74ab0c64a21
KAKAO_CLIENT_ID=bdb424ac8d5355e8797e2f8b7cf05d5c
KAKAO_CLIENT_SECRET=WivbZZpfKJJQe3hpp5sNslbpFZBBx4gd
GOOGLE_CLIENT_ID=117898675158-...
GOOGLE_CLIENT_SECRET=GOCSPX-...
```

---

### 📄 .dockerignore
> Docker 빌드 시 **제외할 파일/폴더** (빌드 속도 향상)

```
.git
.idea
build
*.log
node_modules
```

---

## 3. 로컬 Docker 배포 순서

### Step 1: 사전 준비
```powershell
# Docker Desktop 설치 및 실행 확인
docker --version
docker-compose --version
```

### Step 2: 이미지 빌드
```powershell
# 프로젝트 폴더로 이동
cd C:\workspace\simple_side\doll_gacha

# 이미지 빌드 (Dockerfile 기반)
docker-compose build
```
**결과**: `doll_gacha-app` 이미지 생성

### Step 3: 컨테이너 실행
```powershell
# 백그라운드에서 컨테이너 실행 (-d = detached)
docker-compose up -d
```
**결과**: 
- `doll_gacha_db` 컨테이너 (MariaDB)
- `doll_gacha_app` 컨테이너 (Spring Boot)

### Step 4: 동작 확인
```powershell
# 실행 중인 컨테이너 확인
docker ps

# 애플리케이션 로그 확인
docker logs doll_gacha_app -f

# 브라우저에서 접속
# http://localhost:8080
```

### Step 5: 중지 및 정리
```powershell
# 컨테이너 중지
docker-compose down

# 컨테이너 + 볼륨(DB 데이터) 전부 삭제
docker-compose down -v

# 이미지 재빌드 (코드 변경 후)
docker-compose build --no-cache
docker-compose up -d
```

---

## 4. 실제 서비스 배포 옵션

### 옵션 A: Docker Hub + 클라우드 VM
```
장점: 완전한 제어권, 학습에 좋음
단점: 서버 관리 필요, 비용 발생

1. Docker Hub에 이미지 푸시
   docker tag doll_gacha-app 내아이디/doll_gacha:latest
   docker push 내아이디/doll_gacha:latest

2. 클라우드 VM(AWS EC2, Oracle Cloud 등)에서
   docker pull 내아이디/doll_gacha:latest
   docker run -d -p 8080:8080 내아이디/doll_gacha:latest
```

### 옵션 B: Railway (추천 - 초보자용)
```
장점: GitHub 연동만으로 자동 배포, 무료 티어 있음
단점: 무료는 제한적, 한국 리전 없음

1. railway.app 가입
2. New Project → Deploy from GitHub repo
3. 환경변수 설정 (Settings → Variables)
4. 자동 배포 완료!
```

### 옵션 C: Render
```
장점: 무료 티어, 간편한 설정
단점: 무료는 15분 유휴 시 슬립

1. render.com 가입
2. New → Web Service → GitHub 연결
3. Docker 선택, 환경변수 설정
```

### 옵션 D: AWS ECS / Google Cloud Run
```
장점: 프로덕션급 안정성
단점: 복잡함, 비용 발생

대규모 서비스에 적합
```

---

## 5. 트러블슈팅

### 포트 충돌 에러
```
Error: bind: Only one usage of each socket address
```
**해결**: 해당 포트 사용 중인 프로세스 종료
```powershell
netstat -ano | findstr :8080
taskkill /PID [프로세스ID] /F
```

### OAuth2 로그인 실패
**원인**: 
1. 환경변수 미설정
2. 카카오/구글 개발자 콘솔에서 Redirect URI 미등록

**해결**:
- `.env` 파일에 CLIENT_ID, CLIENT_SECRET 확인
- 카카오 개발자 콘솔 → Redirect URI에 `http://localhost:8080/login/oauth2/code/kakao` 추가

### 이미지/파일 404 에러
**원인**: 볼륨 마운트 문제

**해결**: docker-compose.yml에서 볼륨 확인
```yaml
volumes:
  - ./uploads:/app/uploads  # 호스트의 uploads ↔ 컨테이너의 /app/uploads
```

### DB 연결 실패
**원인**: DB 컨테이너 미준비 상태에서 앱 시작

**해결**: healthcheck + depends_on 사용 (현재 설정됨)

---

## 📌 체크리스트

- [ ] Docker Desktop 설치 및 실행
- [ ] `.env` 파일에 환경변수 설정
- [ ] `docker-compose build` 성공
- [ ] `docker-compose up -d` 성공
- [ ] `http://localhost:8080` 접속 확인
- [ ] OAuth2 로그인 테스트
- [ ] 파일 업로드/다운로드 테스트

---

## 참고 명령어 모음

```powershell
# 이미지 목록
docker images

# 컨테이너 목록 (실행 중)
docker ps

# 컨테이너 목록 (전체)
docker ps -a

# 컨테이너 로그
docker logs [컨테이너명] -f

# 컨테이너 내부 접속
docker exec -it [컨테이너명] /bin/sh

# 이미지 삭제
docker rmi [이미지명]

# 사용하지 않는 리소스 정리
docker system prune -a
```

